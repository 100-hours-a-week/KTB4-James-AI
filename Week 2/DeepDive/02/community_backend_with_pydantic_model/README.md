
# community_backend_with_pydantic_model

## 적용 포인트
- `PostIn`, `CommentIn`, `ChatRequest` 검증 강화
- provider 화이트리스트, prompt 길이 제한, temperature 범위 제한

## 실행
```bash
uvicorn main:app --reload --port 8202
```
